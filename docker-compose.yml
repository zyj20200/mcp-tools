version: '3.8'
services:
  mcp-tools:
    build:
      context: ../
      dockerfile: mcp-tools/Dockerfile
    image: mcp-tools:latest
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
